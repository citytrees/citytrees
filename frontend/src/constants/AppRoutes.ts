const AppRoutes = {
    LOGIN: '/login',
    REGISTER: '/register',
    PASSWORD_RESET: '/password/reset',
    MAIN: '/',
    USER_EMAIL_CONFIRMATION: "/user/confirm"
} as const

export default AppRoutes;